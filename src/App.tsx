import { useEffect, useState } from 'react';
import { NavLink, Route, Routes, useNavigate } from 'react-router-dom';
import { BookOpen, Brain, CalendarDays, ClipboardList, Sigma, GraduationCap, LayoutDashboard, Menu, Star, X } from 'lucide-react';
import { BarChart, Bar, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import questionsSource from './data/questions.json'; import formulas from './data/formulas.json'; import knowledge from './data/knowledge.json';
import type { Question } from './types'; import { askAI } from './services/aiService'; import { useLearningStore } from './store/useLearningStore';
import { getSession, isCloudConfigured, signIn, signOut, signUp } from './services/authService';
const questions = Array.from({length:120},(_,i)=>({...questionsSource[i%questionsSource.length],id:`${questionsSource[i%questionsSource.length].id}-${i+1}`, difficulty:(['基础','进阶','挑战'][i%3] as Question['difficulty'])}));
const nav=[['/','学习概览',LayoutDashboard],['/knowledge','知识中心',BookOpen],['/questions','智能题库',ClipboardList],['/exam','考试系统',GraduationCap],['/mistakes','错题中心',X],['/formulas','公式中心',Sigma],['/plan','学习计划',CalendarDays],['/ai','AI 教师',Brain]] as const;
function Layout({children}:{children:React.ReactNode}){const [open,setOpen]=useState(false);return <div className="min-h-screen"><header className="lg:hidden flex h-16 items-center justify-between bg-white px-4 border-b"><b className="text-brand-600">PhysicsAssistant</b><button onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></header><aside className={`${open?'block':'hidden'} lg:block fixed z-20 lg:sticky top-0 h-screen w-64 bg-white border-r border-slate-100 p-5`}><div className="mb-9 flex gap-3 items-center"><span className="grid h-10 w-10 place-items-center rounded-xl bg-brand-600 text-white"><GraduationCap/></span><div><b>PhysicsAssistant</b><p className="text-xs text-slate-400">大学物理 Pro Max</p></div></div><nav className="space-y-1">{nav.map(([to,label,Icon])=><NavLink end={to==='/'} key={to} to={to} onClick={()=>setOpen(false)} className="nav-item"><Icon size={18}/>{label}</NavLink>)}</nav><div className="absolute bottom-6 text-xs text-slate-400">本地学习数据已自动保存</div></aside><main className="lg:ml-64 p-4 sm:p-7 max-w-[1600px]">{children}</main></div>}
const Title=({title,desc}:{title:string;desc:string})=><div className="mb-6"><h1 className="text-2xl font-bold">{title}</h1><p className="mt-1 text-sm text-slate-500">{desc}</p></div>;
function Dashboard(){const {mistakes}=useLearningStore();const data=[{n:'力学',v:78},{n:'热学',v:64},{n:'电磁',v:52},{n:'光学',v:71}];return <><Title title="早上好，林同学 👋" desc="让每一次练习，都变成更清晰的物理直觉。"/><section className="grid grid-cols-2 lg:grid-cols-4 gap-4">{[['学习等级','Lv. 6 · 探索者'],['连续学习','12 天'],['累计时长','18.5 小时'],['完成题数','126 题']].map(x=><div className="card p-5" key={x[0]}><p className="text-sm text-slate-500">{x[0]}</p><b className="mt-2 block text-xl">{x[1]}</b></div>)}</section><section className="grid lg:grid-cols-3 gap-5 mt-5"><div className="card p-5 lg:col-span-2"><h2 className="font-bold">能力雷达 · 章节掌握度</h2><div className="h-64 mt-4"><div className="h-64 w-full mt-4">
  <ResponsiveContainer width="100%" height="100%">
    <BarChart data={data}><XAxis dataKey="n"/><YAxis domain={[0,100]}/><Tooltip/><Bar dataKey="v" fill="#2563eb" radius={[7,7,0,0]}/></BarChart></ResponsiveContainer></div></div><div className="card p-5 bg-gradient-to-br from-blue-600 to-indigo-700 text-white"><p className="text-blue-100">今日任务</p><h2 className="mt-3 text-xl font-bold">静电场高斯定理</h2><p className="mt-2 text-sm text-blue-100">阅读 12 分钟 · 练习 8 题</p><NavLink to="/questions" className="mt-5 inline-block rounded-xl bg-white px-4 py-2 text-sm font-medium text-blue-700">开始训练</NavLink></div></section><section className="grid lg:grid-cols-2 gap-5 mt-5"><div className="card p-5"><h2 className="font-bold">AI 学习建议</h2><p className="mt-3 text-sm leading-6 text-slate-600">电磁学是当前薄弱区。建议先复习“高斯定理的对称性选择”，再完成 5 道基础题。{Object.keys(mistakes).length>0&&` 你已有 ${Object.keys(mistakes).length} 道错题可复盘。`}</p></div><div className="card p-5"><h2 className="font-bold">今日进度</h2><div className="mt-4 h-3 overflow-hidden rounded-full bg-slate-100"><div className="h-full w-[65%] rounded-full bg-brand-600"/></div><p className="mt-2 text-sm text-slate-500">已完成 2 / 3 项任务</p></div></section></>}
function Knowledge(){const [selected,setSelected]=useState(knowledge[0]);return <><Title title="知识中心" desc="按讲义章节构建的大学物理知识网络。"/><div className="grid lg:grid-cols-3 gap-5"><div className="card p-3">{knowledge.map(k=><button key={k.id} onClick={()=>setSelected(k)} className={`w-full text-left rounded-xl p-4 mb-1 ${selected.id===k.id?'bg-brand-50 text-brand-600':'hover:bg-slate-50'}`}><span className="text-xs">{k.category}</span><b className="block mt-1">{k.name}</b></button>)}</div><article className="card p-6 lg:col-span-2"><span className="text-sm text-brand-600">{selected.category}</span><h2 className="text-2xl font-bold mt-1">{selected.name}</h2><p className="mt-5 leading-7 text-slate-600">{selected.definition}</p><h3 className="font-bold mt-6">核心公式</h3><div className="flex gap-2 flex-wrap mt-2">{selected.formulas.map(f=><code className="rounded-lg bg-blue-50 px-3 py-2 text-brand-600" key={f}>{f}</code>)}</div><h3 className="font-bold mt-6">理解与推导</h3><p className="mt-2 text-slate-600">{selected.derivation}</p><h3 className="font-bold mt-6">易错点</h3><ul className="mt-2 list-disc pl-5 text-slate-600">{selected.traps.map(t=><li key={t}>{t}</li>)}</ul></article></div></>}
function Questions(){const [search,setSearch]=useState('');const [chapter,setChapter]=useState('全部');const {favorites,toggleFavorite}=useLearningStore();const list=questions.filter(q=>(chapter==='全部'||q.chapter===chapter)&&q.question.includes(search));return <><Title title="智能题库" desc={`已加载 ${questions.length} 道本地题目，支持分类、收藏与练习。`}/><div className="flex gap-3 flex-wrap mb-5"><input className="input" value={search} onChange={e=>setSearch(e.target.value)} placeholder="搜索题目关键词"/>{['全部','力学','热学','电磁学','光学','近代物理'].map(c=><button onClick={()=>setChapter(c)} key={c} className={chapter===c?'btn':'btn-soft'}>{c}</button>)}</div><div className="space-y-3">{list.slice(0,24).map(q=><div className="card p-5" key={q.id}><div className="flex justify-between gap-3"><span className="text-xs text-brand-600">{q.chapter} · {q.knowledgePoint} · {q.difficulty}</span><button onClick={()=>toggleFavorite(q.id)} className="text-amber-400"><Star size={19} fill={favorites.includes(q.id)?'currentColor':'none'}/></button></div><p className="mt-2 font-medium">{q.question}</p><div className="mt-3 grid sm:grid-cols-2 gap-2">{q.options.map((o,i)=><span key={o} className="rounded-lg bg-slate-50 px-3 py-2 text-sm">{String.fromCharCode(65+i)}. {o}</span>)}</div></div>)}</div></>}
function Exam(){const navigate=useNavigate();const {answers,saveAnswer,submit,resetExam}=useLearningStore();const exam=questions.slice(0,10);const done=exam.filter(q=>answers[q.id]?.submitted).length;const score=exam.reduce((s,q)=>s+(answers[q.id]?.submitted&&answers[q.id].selected===q.answer?10:0),0);return <><Title title="考试系统" desc="模拟期末考试 · 10 题 · 20 分钟 · 自动保存"/><div className="grid lg:grid-cols-[1fr_280px] gap-5"><div className="space-y-4">{exam.map((q,i)=>{const a=answers[q.id];return <div className="card p-5" key={q.id}><b className="text-brand-600">{i+1}. </b>{q.question}<div className="mt-4 grid gap-2">{q.options.map((o,j)=><button onClick={()=>saveAnswer(q.id,j)} key={o} className={`rounded-xl border p-3 text-left text-sm ${a?.selected===j?'border-brand-500 bg-brand-50':'border-slate-200'}`}>{String.fromCharCode(65+j)}. {o}</button>)}</div>{a?.submitted&&<p className={`mt-3 text-sm ${a.selected===q.answer?'text-emerald-600':'text-red-500'}`}>{a.selected===q.answer?'回答正确！':'答案：'+String.fromCharCode(65+q.answer)+'。'+q.analysis}</p>}</div>})}</div><aside className="card h-fit p-5 lg:sticky top-5"><h2 className="font-bold">答题卡</h2><div className="mt-3 grid grid-cols-5 gap-2">{exam.map((q,i)=><span className={`grid h-9 place-items-center rounded-lg text-sm ${answers[q.id]?'bg-brand-100 text-brand-600':'bg-slate-100'}`} key={q.id}>{i+1}</span>)}</div><p className="mt-5 text-sm text-slate-500">已完成 {done} / {exam.length}</p><button className="btn w-full mt-3" onClick={()=>{exam.forEach(q=>submit(q.id,answers[q.id]?.selected===q.answer));}}>提交并评分</button>{done===exam.length&&<><p className="mt-4 font-bold text-lg">成绩：{score} / 100</p><button className="btn-soft mt-2 w-full" onClick={()=>{resetExam();navigate('/exam')}}>重新开始</button></>}</aside></div></>}
function Mistakes(){const {mistakes}=useLearningStore();const list=questions.filter(q=>mistakes[q.id]);return <><Title title="错题中心" desc="错题会随考试提交自动记录到本地。"/>{list.length===0?<div className="card p-8 text-center text-slate-500">还没有错题。完成一场考试后，这里会生成针对性的复习清单。</div>:<div className="space-y-3">{list.map(q=><div className="card p-5" key={q.id}><span className="text-sm text-red-500">累计错误 {mistakes[q.id]} 次</span><p className="mt-2 font-medium">{q.question}</p><p className="mt-3 text-sm text-slate-600">错误分析：{q.mistakeReason}</p><p className="mt-2 text-sm text-brand-600">推荐复习：{q.knowledgePoint} · {q.formula}</p></div>)}</div>}</>}
function Formulas(){const [cat,setCat]=useState('全部');const shown=formulas.filter(f=>cat==='全部'||f.category===cat);return <><Title title="公式中心" desc="按适用条件理解公式，而不是孤立记忆。"/><div className="mb-4 flex gap-2 flex-wrap">{['全部','力学','热学','电磁学','光学','近代物理'].map(x=><button onClick={()=>setCat(x)} className={cat===x?'btn':'btn-soft'} key={x}>{x}</button>)}</div><div className="grid md:grid-cols-2 xl:grid-cols-3 gap-4">{shown.map(f=><article className="card p-5" key={f.id}><span className="text-xs text-brand-600">{f.category}</span><h2 className="font-bold mt-1">{f.name}</h2><code className="block mt-4 rounded-lg bg-slate-50 p-3 text-brand-600">{f.expression}</code><p className="mt-3 text-sm text-slate-600">变量：{f.variables}</p><p className="mt-2 text-sm text-slate-600">条件：{f.condition}</p><p className="mt-2 text-sm text-red-500">常见错误：{f.errors}</p></article>)}</div></>}
function Plan(){const [date,setDate]=useState('2026-09-01');const [level,setLevel]=useState('中等');const [score,setScore]=useState(85);const days=Math.max(1,Math.ceil((new Date(date).getTime()-Date.now())/86400000));const daily=Math.max(8,Math.ceil(120/days));return <><Title title="学习计划" desc="依据你的考试日期、当前水平与目标分数生成学习节奏。"/><div className="card p-6 max-w-3xl"><div className="grid sm:grid-cols-3 gap-4"><label className="text-sm">考试日期<input type="date" className="input mt-2 w-full" value={date} onChange={e=>setDate(e.target.value)}/></label><label className="text-sm">当前水平<select className="input mt-2 w-full" value={level} onChange={e=>setLevel(e.target.value)}><option>基础</option><option>中等</option><option>扎实</option></select></label><label className="text-sm">目标分数<input type="number" className="input mt-2 w-full" value={score} onChange={e=>setScore(+e.target.value)}/></label></div><div className="mt-7 rounded-2xl bg-brand-50 p-5"><b>你的计划：距考试 {days} 天 · 每日 {daily} 题</b><p className="mt-3 text-sm leading-6 text-slate-600">第 1 阶段（40%）：{level==='基础'?'夯实概念与公式卡片':'复盘薄弱章节'}；第 2 阶段（40%）：章节专项训练；第 3 阶段（20%）：整卷模拟与错题回炉。目标 {score} 分时，每周至少安排 1 次限时考试。</p></div></div></>}
function AITutor(){const [mode,setMode]=useState<'知识解释'|'公式解释'|'题目解析'|'学习建议'>('知识解释');const [prompt,setPrompt]=useState('');const [answer,setAnswer]=useState('');const [loading,setLoading]=useState(false);async function send(){if(!prompt.trim())return;setLoading(true);setAnswer(await askAI({mode,prompt}));setLoading(false)}return <><Title title="AI 教师" desc="Mock AI 服务已就绪，可在 services/aiService.ts 替换为真实 API。"/><div className="grid lg:grid-cols-[1fr_340px] gap-5"><section className="card p-6"><div className="flex gap-2 flex-wrap">{(['知识解释','公式解释','题目解析','学习建议'] as const).map(x=><button key={x} onClick={()=>setMode(x)} className={mode===x?'btn':'btn-soft'}>{x}</button>)}</div><textarea className="input mt-5 h-32 w-full" value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder="例如：为什么高斯定理只适合高对称性的场？"/><button className="btn mt-3" onClick={send}>{loading?'正在思考…':'请 AI 讲解'}</button>{answer&&<div className="mt-5 whitespace-pre-wrap rounded-xl bg-slate-50 p-5 leading-7 text-slate-700">{answer}</div>}</section><aside className="card p-5"><h2 className="font-bold">可以这样问</h2><ul className="mt-3 space-y-3 text-sm text-slate-600"><li>“用直觉解释电势和电势能。”</li><li>“帮我检查这题的受力分析。”</li><li>“给我 7 天复习电磁学的建议。”</li></ul></aside></div></>}
function AuthScreen(){const [register,setRegister]=useState(false),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[notice,setNotice]=useState(''),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError('');setNotice('');setBusy(true);try{if(register){await signUp(email,password,name||'物理学习者');setNotice('注册成功，请到邮箱确认后登录。')}else await signIn(email,password)}catch(e){setError(e instanceof Error?e.message:'操作失败')}finally{setBusy(false)}}return <main className="min-h-screen grid place-items-center bg-gradient-to-br from-blue-50 via-white to-indigo-50 p-5"><form onSubmit={submit} className="card w-full max-w-md p-7"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-brand-600 text-white"><GraduationCap/></div><h1 className="mt-5 text-2xl font-bold">大学物理智能学习助手</h1><p className="mt-2 text-sm text-slate-500">{register?'创建账号，开始你的学习闭环。':'登录后跨设备同步练习与错题记录。'}</p>{!isCloudConfigured()&&<div className="mt-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-700">当前是演示模式。部署前请在 Vercel 配置 Supabase 环境变量。</div>}{register&&<input className="input mt-5 w-full" placeholder="昵称" value={name} onChange={e=>setName(e.target.value)}/>}<input className="input mt-3 w-full" type="email" required placeholder="邮箱" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input mt-3 w-full" type="password" required minLength={6} placeholder="密码（至少 6 位）" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}{notice&&<p className="mt-3 text-sm text-emerald-600">{notice}</p>}<button className="btn mt-5 w-full" disabled={busy}>{busy?'处理中…':register?'创建账号':'登录'}</button><button type="button" onClick={()=>setRegister(!register)} className="mt-4 w-full text-sm text-brand-600">{register?'已有账号？去登录':'没有账号？创建账号'}</button><button type="button" onClick={()=>window.dispatchEvent(new Event('physics-demo'))} className="mt-3 w-full text-sm text-slate-400">先进入演示模式</button></form></main>}
function AuthGate(){const [session,setSession]=useState(getSession());const [demo,setDemo]=useState(false);useEffect(()=>{const update=()=>setSession(getSession());window.addEventListener('physics-auth',update);window.addEventListener('physics-demo',()=>setDemo(true));return()=>{window.removeEventListener('physics-auth',update)}},[]);if(!session&&!demo)return <AuthScreen/>;return <><div className="fixed right-4 top-4 z-30 text-xs text-slate-400">{session?.user.email||'演示模式'} {session&&<button onClick={signOut} className="ml-2 text-brand-600">退出</button>}</div><Layout><Routes><Route path="/" element={<Dashboard/>}/><Route path="/knowledge" element={<Knowledge/>}/><Route path="/questions" element={<Questions/>}/><Route path="/exam" element={<Exam/>}/><Route path="/mistakes" element={<Mistakes/>}/><Route path="/formulas" element={<Formulas/>}/><Route path="/plan" element={<Plan/>}/><Route path="/ai" element={<AITutor/>}/></Routes></Layout></>}
export default function App(){return <AuthGate/>}
